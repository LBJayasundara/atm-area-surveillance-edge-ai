rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Alert snapshot images — readable by authenticated users;
    // written only by the server-side Admin SDK (Raspberry Pi).
    match /alerts/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
